Primero inicia la api:

docker compose up --build

docker compose up -d

java -jar target/api-0.0.1-SNAPSHOT.jar

Segundo inicia el proyecto:

java -jar target/backend-0.0.1-SNAPSHOT.jar --api.base.url=http://localhost:8080/api --server.port=8081
